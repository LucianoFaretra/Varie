#include <stdio.h>
#include <stdlib.h>

int ConfrontoColori( char combinazione_codificatore[], char combinazione_decifratore[] )
{
	int esito_pioli;
	int i, j;

	i=0;
	while (i < pioli_codice(?))					//definire pioli codice
		{ j=0;
		while (j < pioli_chiave(?))					//definire pioli chiave
		{
		if ((combinazione_decifratore[i] == combinazione_codificatore[j])
			scrittura_array(esito_pioli, valore = 2);					//così pero ogni volta da un esito (in totale così da 4 esiti)
			else if ((lettura_array(combinazione_decifratore, i) == lettura_array(combinazione_codificatore, j+1) || (combinazione_decifratore[i] == combinazione_codificatore[j+2]) || (combinazione_decifratore[i] == combinazione_codificatore[j+3]))
					scrittura_array(esito_pioli, valore = 1);
					else scrittura_array(esito_pioli, valore = 0);
		j++; }}
		i++;

	return 0;
}
