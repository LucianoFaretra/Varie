#include <stdio.h>
#include <stlib.h>

int main()
{
	char combinazione_codificatore[4];
	char combinazione_decifratore[4];
	int i, j;
	
	i=0;
	while(i < numero_spazi(?))					//definire numero spazi
		{ combinazione_codificatore[i] = combinazione_casuale(?);			//ho pensato di mettere tutte le possibile combinazioni in or, col rischio però che in caso di single player e quindi combinazione casuale, il sistema prenda sempre la prima disponibile
		combinazione_codificatore[i] = combinazione_casuale;			//assegnazione della combinazione
		i++;
		}
		
	return 0;
}
